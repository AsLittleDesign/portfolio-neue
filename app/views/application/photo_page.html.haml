
- content_for :head do
  = stylesheet_link_tag "pages/photo_page", media: "all", "data-turbolinks-track" => true

.container.photo--top
  = link_to photography_path, :class => "button m-blue" do
    < Photography

.container
  = image_tag @photo["url"], :class => "photo--image"
  %h2= @photo["title"]
  %p= @photo["description"]
